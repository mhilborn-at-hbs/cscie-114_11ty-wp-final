---
pagination:
    data: wpposts
    size: 1
    alias: post
    addAllPagesToCollections: true
permalink: "posts/{{ post.id | slugify}}-{{ post.slug | slugify }}/"
title: post.title.rendered
layout: base.liquid
tags: posts
---

<h2>{{ post.title.rendered }}</h2>
<div>{{ post.content.rendered }}</div>

<p>Authored on: {{ post.date }}</p>