---
title: A Blog About Star Wars Movies
layout: base.liquid
---

<h2>{{ title }}</h2>
<div id="homepage">
  <ul>
    {% for post in collections.posts %}
      <li>
        <a href="{{ post.url }}">{{ post.data.post.title.rendered }}</a><br/>
        <div class="postListDate">{{ post.data.post.date }}</div>
        <div class="postListExcerpt">{{ post.data.post.excerpt.rendered }}</siv>
      </li>
    {% endfor %}
  </ul>
</div>